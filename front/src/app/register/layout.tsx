import { FC } from "react";

interface LayoutProps{

    children: React.ReactElement
}

const RegisterLayout:FC<LayoutProps> = ({children} ) => {
    return ( 
        children

     );
}
 
export default RegisterLayout;